#!/usr/bin/env groovy
pipeline {
  agent any
  
  stages {
    stage('build') {
      steps {
        sh 'echo Fake build'
      }
    }

    stage('test') {
      steps {
        sh 'echo Fake test'
      }
    }
  }
}